import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      // In LOCAL DEV: all /api calls are forwarded to local backend
      // In PRODUCTION BUILD: this proxy does NOT exist — VITE_API_URL is used instead
      '/api': {
        target: process.env.VITE_API_URL || 'http://localhost:5000',
        changeOrigin: true,
        secure: false,
      },
    },
  },
  build: {
    outDir: 'dist',
    sourcemap: false,
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom', 'react-router-dom'],
          charts: ['recharts'],
        },
      },
    },
  },
  // Make env vars available — only VITE_ prefixed vars are exposed to browser
  envPrefix: 'VITE_',
});
